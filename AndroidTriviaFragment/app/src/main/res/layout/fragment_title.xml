<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout

        android:id="@+id/titleConstraint"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".TitleFragment">

        <ImageView
            android:id="@+id/iv_title_image"
            android:layout_width="@dimen/_0"
            android:layout_height="@dimen/_192"
            android:layout_marginStart="@dimen/_8"
            android:layout_marginTop="152dp"
            android:layout_marginEnd="@dimen/_8"
            android:contentDescription="@string/trivia_image"
            android:src="@drawable/android_trivia"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btn_play"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="92dp"
            android:text="@string/play"
            app:layout_constraintEnd_toEndOf="@+id/iv_title_image"
            app:layout_constraintHorizontal_bias="0.498"
            app:layout_constraintStart_toStartOf="@+id/iv_title_image"
            app:layout_constraintTop_toBottomOf="@+id/iv_title_image" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
